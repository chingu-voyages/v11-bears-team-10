import { combineReducers } from 'redux';

// the testdata was only passed to implement
// the project skeleton
const rootReducer = combineReducers({
   testReducerData: (state = {}) => state
});

export default rootReducer;